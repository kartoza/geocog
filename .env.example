# Copy to .env (or just run `nix run .#upload`, which creates it for you).
# .env is gitignored AND Claude-ignored — never commit real credentials.
MINIO_ENDPOINT='https://api.minio.do.kartoza.com'
MINIO_ACCESS_KEY=''
MINIO_SECRET_KEY=''
MINIO_BUCKET='tim-deleteme'
MINIO_PREFIX='dgt'
